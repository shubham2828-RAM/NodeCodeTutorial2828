<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            background: #3d5fdbc1;
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SignUp</title>
</head>
<body>
    <h1>Sign Up</h1>
    <% console.log("hiii buddyy yoooo",locals.username) %>
    <% if(locals.username && locals.email && locals.userId ) { %>
        <p> 
            Sign Up Successful for <%= username %> with email <%= email %> and UserId = <%= userId %>
            <br/>
            You can now <a href="/user/login">Login</a>
        </p>
    <% } else { %>
        <div>
            <form method="POST" action="/user/signup/details">
                <label>Full Name:</label>
                <input type="text" name="username" placeholder="Enter Full Name" style="margin: 5px;" />
                <br/>
                <label>Email:</label>
                <input type="text" name="email" placeholder="Enter Email" style="margin: 5px;" />
                <br/>
                <label>Password:</label>
                <input type="password" name="password" placeholder="Enter Password" style="margin: 5px;" />
                <br/>
                <button type="submit">Sign Up</button>
            </form>
        </div>
    <% } %>
</body>
</html>